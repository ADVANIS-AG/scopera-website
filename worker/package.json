{
  "name": "scopera-lead-gateway",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "kv:create-sessions": "wrangler kv namespace create SESSIONS",
    "kv:create-admin-store": "wrangler kv namespace create ADMIN_STORE"
  },
  "dependencies": {
    "jose": "^5.9.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260730.1",
    "typescript": "^5.5.0",
    "wrangler": "^4.0.0"
  }
}
